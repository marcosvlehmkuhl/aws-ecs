on:
  push:
      branches:
          - master
name: Deploy dev branch
jobs:
  deploy:
      name: deploy
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v2
          - uses: actions/setup-node@v1
            with:
                node-version: '12'

          - name: docker
            run: docker build .
